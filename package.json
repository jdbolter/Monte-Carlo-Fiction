{
  "name": "monte-carlo-fiction",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Distant-writing world model: sample a space of possible media presents (changed dimensions) and ground each in real historical backstory, producing one World object that many renderers can turn into artifacts.",
  "scripts": {
    "sample": "node sampling/sampler.js",
    "backstory": "node sampling/backstory.js",
    "world": "node sampling/world.js",
    "render": "node sampling/render-config.js",
    "main": "node sampling/main-server.js",
    "ui": "node sampling/server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
