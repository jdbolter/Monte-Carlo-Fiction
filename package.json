{
  "name": "monte-carlo-fiction",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "description": "Generate structured alternate-present worlds from researched historical corpora and user-supplied divergences, then render selected worlds into narrative forms.",
  "scripts": {
    "dev": "node sampling/main-server.js",
    "main": "node sampling/main-server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  }
}
