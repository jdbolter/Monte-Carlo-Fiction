{
  "name": "monte-carlo-fiction",
  "version": "0.5.0",
  "private": true,
  "type": "module",
  "description": "Generate structured alternate histories and future scenarios from researched historical lineages, then render selected Worlds.",
  "scripts": {
    "dev": "node sampling/main-server.js",
    "main": "node sampling/main-server.js",
    "test": "node --test",
    "validate:corpora": "node sampling/validate-corpora.js"
  },
  "engines": {
    "node": ">=18"
  }
}
